version: '3'

services:
  web:
    image: jitsi/web:stable
    restart: unless-stopped
    ports:
      - "8080:80"
    environment:
      - ENABLE_AUTH=0
      - ENABLE_GUESTS=1
      - XMPP_DOMAIN=meet.jitsi
      - XMPP_SERVER=xmpp.meet.jitsi
